# Kernel
arm_64bit=1
kernel=Image

# Ramdisk
initramfs ramdisk.img followkernel

# Audio
dtparam=audio=on
dtoverlay=tc358743-audio

# Bluetooth
dtparam=krnbt=on

# Camera
# camera_auto_detect=1 detects native CSI sensors (imx219, ov5647, imx477...)
# For HDMI-to-CSI bridge (tc358743), disable auto_detect and load overlay manually:
#   dtoverlay=tc358743
# media-controller=0: unicam links pipeline internally (no media-ctl needed)
# media-controller=1: requires MEDIA_IOC_SETUP_LINK via setup_media_links tool
#camera_auto_detect=1
dtoverlay=tc358743,media-controller=0
start_x=1

# CPU
arm_boost=1

# Display
disable_overscan=1

# Display panel
#dtoverlay=vc4-kms-dsi-7inch
#dtoverlay=rpi-backlight

# Graphics acceleration
disable_fw_kms_setup=1
dtoverlay=vc4-kms-v3d
dtoverlay=cma,cma-512

# I2C
#dtparam=i2c_arm=on

# I2S
#dtparam=i2s=on

# Serial console
enable_uart=1

# SPI
#dtparam=spi=on

# USB
dtoverlay=dwc2,dr_mode=otg
